## Codebase Patterns
- Use `modernc.org/sqlite` (pure Go, no CGO) for SQLite
- Database package lives in `server/db/` with `Open(storagePath)` constructor
- Clip model uses `*string` for nullable fields (e.g., Filename)
- ID generation: 6-char alphanumeric via crypto/rand
- Tests use `t.TempDir()` for isolated DB instances
- SQLite connection uses `SetMaxOpenConns(1)` for write safety
- Handlers live in `server/handlers/` with struct-based dependency injection (DB, config)
- Gin route order matters: `/clips/latest` must be registered before `/clips/:id` to avoid param conflict
- Use `c.File()` for streaming file content in Gin (handles range requests automatically)
- Clip API returns `clipResponse` (subset of Clip) to avoid leaking internal fields like StoragePath
- Chunked uploads store parts in `STORAGE_PATH/uploads/<id>/part_N`, merged on complete to `STORAGE_PATH/<id>/<filename>`
- `UpdateClip(id, status, sizeBytes, storagePath)` updates clip record after chunked upload completes
- Cleanup package in `server/cleanup/` runs background goroutine with `Start(db, storagePath, interval)`
- `GetExpiredClips()` returns all clips where `expires_at < now()` (both ready and uploading)
- `GetStorageStats()` returns `{ClipCount, StorageUsedBytes}` via SQL COUNT/SUM
- macOS app uses Swift Package Manager (not .xcodeproj) in `macos/CopyEverywhere/`
- Build with `swift build` from `macos/CopyEverywhere/` directory
- MenuBarExtra with `.window` style creates popover panel from menu bar icon
- `NSApp.setActivationPolicy(.accessory)` hides dock icon programmatically
- Keychain access via Security framework: `SecItemAdd`/`SecItemCopyMatching`/`SecItemDelete`
- ConfigStore is `@MainActor` ObservableObject shared via `.environmentObject()`

---

## 2026-04-11 - US-002
- What was implemented: SQLite database schema with clips table, Clip model struct, and all CRUD helper functions (CreateClip, GetClipByID, GetLatestClip, ListClips, DeleteClip) plus 6-char alphanumeric ID generation
- Files changed:
  - `server/db/db.go` (new) - Database initialization, schema migration, model, and helpers
  - `server/db/db_test.go` (new) - 9 tests covering all helpers, ID generation, null fields, and DB creation
  - `server/go.mod` / `server/go.sum` - Added modernc.org/sqlite dependency
- **Learnings for future iterations:**
  - `modernc.org/sqlite` is a pure Go SQLite driver, no CGO required - good for cross-compilation
  - GetLatestClip filters by `status = 'ready'` AND `expires_at > now()` - important for future API endpoints
  - Clip.Filename is `*string` (nullable) - text clips don't need a filename
  - The `Open()` function auto-creates the storage directory and runs migrations with `IF NOT EXISTS`
---

## 2026-04-11 - US-003
- What was implemented: Clips API with four endpoints - upload (POST /api/v1/clips), get metadata (GET /api/v1/clips/:id), get latest (GET /api/v1/clips/latest), and download raw content (GET /api/v1/clips/:id/raw)
- Files changed:
  - `server/handlers/clips.go` (new) - ClipHandler struct with Upload, GetByID, GetLatest, GetRaw methods
  - `server/handlers/clips_test.go` (new) - 12 tests covering all endpoints, error cases, size limits, and round-trip
  - `server/main.go` - Added DB initialization and route registration
- **Learnings for future iterations:**
  - Register `/clips/latest` before `/clips/:id` in Gin to avoid route param conflicts
  - Use `clipResponse` struct to avoid leaking internal fields (StoragePath, Status) in API responses
  - `c.File()` in Gin handles Content-Type detection and range requests automatically
  - Handler tests use `httptest.NewRecorder()` + `gin.New()` without middleware for isolation
---

## 2026-04-11 - US-004
- What was implemented: Chunked upload API with four endpoints - init upload (POST /api/v1/uploads/init), upload part (PUT /api/v1/uploads/:id/parts/:n), complete upload (POST /api/v1/uploads/:id/complete), and get status (GET /api/v1/uploads/:id/status)
- Files changed:
  - `server/handlers/uploads.go` (new) - UploadHandler struct with InitUpload, UploadPart, CompleteUpload, GetUploadStatus methods
  - `server/handlers/uploads_test.go` (new) - 10 tests including full round-trip with chunk merge verification
  - `server/db/db.go` - Added UpdateClip helper for updating status, size, and storage path
  - `server/main.go` - Added UploadHandler initialization and route registration
- **Learnings for future iterations:**
  - Chunk parts stored as `part_N` files in `STORAGE_PATH/uploads/<upload_id>/` directory
  - `getPartsInfo` infers total chunk count from first part's file size - works because all chunks except last are same size
  - After complete, parts directory is cleaned up with `os.RemoveAll` and final file moved to `STORAGE_PATH/<clip_id>/`
  - Upload clip record is created at init time with status=uploading, updated to ready on complete
  - The `UpdateClip` DB helper updates status, size_bytes, and storage_path in one call
---

## 2026-04-11 - US-005
- What was implemented: TTL cleanup background goroutine (every 10 minutes) that deletes expired clips from disk and DB, plus enhanced /health endpoint with storage_used_bytes and clip_count fields
- Files changed:
  - `server/cleanup/cleanup.go` (new) - Background cleanup goroutine with Start() entry point
  - `server/db/db.go` - Added GetExpiredClips() and GetStorageStats() methods
  - `server/db/db_test.go` - Added tests for GetExpiredClips and GetStorageStats
  - `server/main.go` - Added cleanup.Start() call and storage stats to /health
- **Learnings for future iterations:**
  - Cleanup runs once immediately at startup then every ticker interval
  - For uploading clips, cleanup removes both the upload parts dir and the DB record
  - StorageStats uses COALESCE(SUM(size_bytes), 0) to handle empty table case
  - /health endpoint gracefully degrades if stats query fails (still returns version/uptime)
---

## 2026-04-11 - US-006
- What was implemented: Multi-stage Dockerfile (golang:1.26-alpine build, alpine:3.20 runtime), docker-compose.yml with relay service exposing port 8080 and volume mount, .dockerignore for excluding unnecessary files
- Files changed:
  - `server/Dockerfile` (new) - Multi-stage build with CGO_ENABLED=0 for static binary
  - `server/docker-compose.yml` (new) - Single relay service with env vars and named volume
  - `server/.dockerignore` (new) - Excludes .git, tests, docs, compose/dockerfile themselves
- **Learnings for future iterations:**
  - Docker not installed on dev machine, so `docker-compose up --build` could not be verified locally
  - Used CGO_ENABLED=0 for static binary since modernc.org/sqlite is pure Go (no CGO needed)
  - docker-compose uses env var substitution with defaults (e.g., `${ACCESS_TOKEN:-changeme}`)
---

## 2026-04-11 - US-007
- What was implemented: macOS SwiftUI menu bar app with MenuBarExtra shell, server config UI with Keychain storage, and Test Connection functionality
- Files changed:
  - `macos/CopyEverywhere/Package.swift` (new) - SPM manifest targeting macOS 13+
  - `macos/CopyEverywhere/Sources/CopyEverywhere/CopyEverywhereApp.swift` (new) - App entry point with MenuBarExtra and AppDelegate for hiding dock icon
  - `macos/CopyEverywhere/Sources/CopyEverywhere/ConfigStore.swift` (new) - ObservableObject managing config, Keychain CRUD, and connection testing
  - `macos/CopyEverywhere/Sources/CopyEverywhere/ConfigView.swift` (new) - Config UI with Host URL, Access Token, Test Connection, Save
  - `macos/CopyEverywhere/Sources/CopyEverywhere/MainPanelView.swift` (new) - Main panel with gear icon to toggle config, quit button
  - `macos/CopyEverywhere/Sources/CopyEverywhere/MenuBarView.swift` (new) - Root view routing between config and main panel
  - `macos/CopyEverywhere/.gitignore` (new) - Excludes .build/ and .swiftpm/
- **Learnings for future iterations:**
  - SPM executable targets can create macOS menu bar apps without Xcode project files
  - `NSApp.setActivationPolicy(.accessory)` is the programmatic way to hide dock icon (no Info.plist needed for SPM builds)
  - MenuBarExtra `.window` style gives a proper popover panel vs `.menu` which gives a dropdown menu
  - ConfigStore uses `@MainActor` annotation since it interacts with UI-bound state
  - Keychain operations: delete-before-add pattern for updates (SecItemUpdate is more complex)
  - URLError codes `.cannotConnectToHost`, `.timedOut` map well to user-friendly error messages
---

## 2026-04-11 - US-008
- What was implemented: macOS send clipboard text feature - clipboard preview in main panel (max 500 chars truncated), Send Clipboard button that POSTs multipart/form-data to /api/v1/clips, success/error display with Retry button
- Files changed:
  - `macos/CopyEverywhere/Sources/CopyEverywhere/ConfigStore.swift` - Added SendStatus enum, ClipResult struct, getClipboardText() and sendClipboardText() methods with multipart upload
  - `macos/CopyEverywhere/Sources/CopyEverywhere/MainPanelView.swift` - Replaced "Ready" placeholder with clipboard preview, send button, and status display
- **Learnings for future iterations:**
  - Server upload API expects multipart/form-data with "type" field and "content" file part (filename required)
  - For text clips, use filename "clipboard.txt" with Content-Type text/plain
  - Build multipart body manually with UUID boundary - no built-in Swift multipart helper
  - Clipboard refresh on `NSApplication.didBecomeActiveNotification` keeps preview current when user switches back to app
  - `SendStatus` enum with associated values (clipID, expiresAt) works well for displaying contextual results
---
