<Window x:Class="CopyEverywhere.FloatingBallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="64" Height="64"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        AllowDrop="True"
        DragEnter="Ball_DragEnter"
        DragOver="Ball_DragOver"
        DragLeave="Ball_DragLeave"
        Drop="Ball_Drop"
        MouseLeftButtonDown="Ball_MouseLeftButtonDown">

    <Grid>
        <Ellipse x:Name="BallEllipse"
                 Width="56" Height="56"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Fill="#3B82F6"
                 Stroke="#2563EB"
                 StrokeThickness="2">
            <Ellipse.Effect>
                <DropShadowEffect ShadowDepth="2" Opacity="0.3" BlurRadius="6"/>
            </Ellipse.Effect>
        </Ellipse>
        <TextBlock Text="CE"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="White"
                   FontSize="16"
                   FontWeight="Bold"
                   IsHitTestVisible="False"/>
    </Grid>
</Window>
